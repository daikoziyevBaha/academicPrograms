const createBtn = document.querySelector('#createBtn')
var personalStatus = document.querySelector("#personal_status");
var curriculumStatus = document.querySelector("#curriculum_status");

console.log(personalStatus.textContent)

// document.addEventListener("DOMContentLoaded", function() {
//     if (personalStatus.textContent === "Заполнен" && curriculumStatus.textContent === "Заполнен"){
//         createBtn.style.cursor = "pointer";
//         createBtn.disabled = false;
//     } else {
//         createBtn.disabled = true;
//         createBtn.style.cursor = "not-allowed";
//     }
// });

